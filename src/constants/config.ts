export const CONFIG = {
  // Number of items per page in paginated grids
  PAGE_SIZE: 20,
  // The default page number
  DEFAULT_PAGE: 1,
  // The maximum page number allowed by the API
  MAX_PAGE: 500,
};
